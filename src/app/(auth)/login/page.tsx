import React from 'react';
import LoginPage from '@/components/auth/LoginForm';

const Page = () => {
    return (
        <div className="container mt-5">
            <LoginPage />
        </div>
    );
};

export default Page;